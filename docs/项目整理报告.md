# 项目整理报告

## 1. 整理后的目录结构

```text
.
├── README.md
├── requirements.txt
├── .env.example
├── .gitignore
├── main.py
├── start_upload_frontend.bat
├── src/
├── docs/
├── data/
│   ├── raw/
│   ├── processed/
│   └── sample/
├── outputs/
│   ├── reports/
│   └── screenshots/
├── assets/
│   └── images/
└── tests/
```

## 2. 移动的文件

- `docs/temp_company_product_0522_1.xlsx` → `data/sample/temp_company_product_0522_1.xlsx`
- `docs/产品标准体系.xlsx` → `data/sample/产品标准体系.xlsx`

## 3. 新建的目录和文件

- `data/sample/`：存放样例 Excel 数据。
- `assets/images/`：预留流程图、页面截图和答辩图片素材。
- `outputs/screenshots/`：预留运行截图和展示截图。
- `docs/项目整理报告.md`：记录本次整理过程。

## 4. 修改的文件

- `README.md`：重写为适合项目交接和课程设计答辩的完整说明。
- `.env.example`：改为与当前代码一致的 Ollama 可选 AI 配置示例。
- `.gitignore`：取消忽略 `docs/`，使项目文档作为正式交接材料保留。
- `requirements.txt`：补充 `python-dotenv`，保证 `.env` 配置可被当前代码读取。
- `docs/工作流程图.md`：移除本机绝对图片路径，改为项目内相对路径说明。

## 5. 修改的路径引用

- README 中的样例输入路径改为 `data/sample/产品标准体系.xlsx`。
- README 中的报告输出路径统一为 `outputs/reports/`。
- 工作流程图文档中的图片建议路径改为 `assets/images/workflow.jpg` 和 `assets/images/agent_workflow.jpg`。

## 6. README 主要章节

README 包含项目简介、项目目标、核心功能、系统架构、目录结构、环境要求、安装步骤、配置说明、运行方式、数据格式、使用流程、诊断方法、AI 辅助分析、输出结果、docs 阅读顺序、当前不足和项目交接说明。

## 7. README 参考的 docs 内容

- `docs/分析/会议总结.md`：课程任务、目标和成果要求。
- `docs/分析/数据结构概况.md`：字段含义、树结构、数据统计和同义词覆盖。
- `docs/分析/问题分析.md`：结构类、内容类、评价类和版本类问题。
- `docs/解决方案.md`：树高度、宽度、平衡、叶子节点占比、内容语义和 AI 抽样分析设计。
- `docs/工作流程问题.md`：当前展示和诊断结果的待优化点。

## 8. 用途无法完全确认的文件

- `data/raw/`、`data/processed/`、`outputs/charts/`、`outputs/issues/` 当前为空目录，保留为后续数据处理和图表输出位置。
- `assets/images/` 当前未包含图片，需人工补充流程图或截图素材。

## 9. 已实现功能

- 数据读取、字段别名兼容和 `category_group_id` 推导父节点。
- 分类树派生字段构建。
- 结构、内容和同义词规则诊断。
- 体系健康评分。
- Markdown / JSON 报告输出。
- 本地 Web 上传诊断和 HTML 看板展示。
- 可选本地 Ollama 语义分析。
- 基础版本对比。
- 单元测试。

## 10. 设计目标或后续优化

- 自动修复分类体系问题。
- 完整版本管理、修改日志和回滚。
- 在线 OpenAI / Google API 自动诊断调用。
- 大规模数据性能优化。
- AI 输出稳定性和置信度展示优化。
- 前端表格展示、问题排序和指标中文解释优化。

## 11. 需要人工确认的地方

- `docs/工作流程图.md` 原本引用的本地图片文件缺失，需要人工补充到 `assets/images/`。
- 规则阈值需要结合更多真实数据继续调参。
- AI 语义分析结果需要人工复核，不应直接作为自动修改依据。
