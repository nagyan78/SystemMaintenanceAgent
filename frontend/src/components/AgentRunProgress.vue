<template><section class="card"><div class="card-head"><div><p class="eyebrow">候选执行单元</p><h2>Agent 运行进度</h2></div><span class="badge">剩余 {{ counts.remaining }}</span></div><div class="run-grid"><div><span>候选总数</span><strong>{{ counts.total }}</strong></div><div><span>已处理</span><strong>{{ counts.processed }}</strong></div><div><span>发现问题</span><strong>{{ counts.issues }}</strong></div><div><span>正常</span><strong>{{ counts.clean }}</strong></div><div><span>不确定</span><strong>{{ counts.inconclusive }}</strong></div><div><span>失败</span><strong>{{ counts.failed }}</strong></div></div></section></template>
<script setup lang="ts">export type AgentRunCounts={total:number;processed:number;issues:number;clean:number;inconclusive:number;failed:number;remaining:number};defineProps<{counts:AgentRunCounts}>()</script>
<style scoped>.run-grid{display:grid;grid-template-columns:repeat(6,1fr);gap:10px}.run-grid div{padding:12px;border-radius:12px;background:rgba(17,24,39,.04)}.run-grid span{display:block;color:var(--muted);font-size:12px}.run-grid strong{display:block;margin-top:5px;font-size:20px}@media(max-width:900px){.run-grid{grid-template-columns:repeat(3,1fr)}}</style>
